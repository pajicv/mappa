#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mapshaper/bin/mapshaper" "$@"
  ret=$?
else 
  node  "$basedir/../mapshaper/bin/mapshaper" "$@"
  ret=$?
fi
exit $ret
